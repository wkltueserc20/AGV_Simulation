## 1. 前端狀態管理與 UI 控制面板實作

- [ ] 1.1 在 `App.tsx` 中定義背景地圖的配置狀態類型 `BackgroundSettings`。
- [ ] 1.2 實作 `localStorage` 的載入與自動保存邏輯（包含 Base64 圖檔與位置參數）。
- [ ] 1.3 在左側邊欄新增「背景地圖配置」控制面板，包含：
  - 檔案上傳元件（`<input type="file" accept="image/*" />`）。
  - 顯示背景圖開關與參數鎖定開關。
  - 透明度滑桿（0~100%）。
  - 真實物理寬度與高度輸入框（附「鎖定寬高比例」開關）。
  - 水平偏移 X 與垂直偏移 Y 輸入框與微調按鈕（公尺單位，換算為 mm）。
  - 旋轉角度輸入框與微調按鈕（0~360°）。
  - 重設與移除地圖的快捷按鈕。
- [ ] 1.4 新增美觀的 CSS 樣式（毛玻璃質感、響應式滑桿、微互動動畫）。

## 2. Canvas 背景圖渲染與變換邏輯

- [ ] 2.1 修改 `SimulatorCanvas.tsx`，為元件新增 `bgImageSrc` 與 `bgSettings` 的 Props 傳遞。
- [ ] 2.2 在 Canvas 內部透過 `useEffect` 非同步載入 `Image` 物件，並在載入完成、參數更動時，將 `staticNeedsUpdate.current` 設為 true。
- [ ] 2.3 在 `updateStaticLayer` 函數中最底層加入圖片繪製程式碼，套用平移、旋轉、縮放與透明度轉換矩陣。

## 3. 功能驗證與測試

- [ ] 3.1 驗證圖片上傳功能：確認不同解析度、格式的 PNG/JPG 均可成功載入。
- [ ] 3.2 驗證等比例鎖定：確認輸入寬度後，高度會依據原始圖片比例自動更新。
- [ ] 3.3 驗證對齊精準度：確認在設定真實長寬與 X/Y 偏移後，背景圖可以與虛擬點狀網格完美對齊。
- [ ] 3.4 驗證效能流暢度：確認在啟用地圖背景的情況下，AGV 動畫幀率依然保持穩定，無卡頓現象。
- [ ] 3.5 驗證持久化：重新整理瀏覽器網頁，確認背景地圖與所有的微調參數均能完整回復。
